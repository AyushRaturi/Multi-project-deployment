apiVersion: v1
kind: Service
metadata:
  name: {{ include "qr-app-chart.fullname" . }}
  labels:
    {{- include "qr-app-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "qr-app-chart.selectorLabels" . | nindent 4 }}
