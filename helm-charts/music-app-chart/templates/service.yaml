apiVersion: v1
kind: Service
metadata:
  name: {{ include "music-app-chart.fullname" . }}
  labels:
    {{- include "music-app-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "music-app-chart.selectorLabels" . | nindent 4 }}
